var React = require('react'),
    SmashFormItem = require('./components/smash_form_item.jsx'),
    Formsy = require('formsy-react');

var SmashForm = React.createClass({
  render: function () {
    return(
    );
  }
});

module.exports = SmashForm;
